// Generated by CoffeeScript 1.4.0
(function() {
  var isList, isString, isSymbol, stringOf, symbolOf;

  isSymbol = function(exp, env) {
    return exp instanceof Object && exp.hasOwnProperty('symbol');
  };

  symbolOf = function(sym) {
    return function(exp, env) {
      return isSymbol(exp, env) && exp.symbol === sym;
    };
  };

  isString = function(exp, env) {
    return exp instanceof Object && exp.hasOwnProperty('string');
  };

  stringOf = function(str) {
    return function(exp, env) {
      return isString(exp, env) && exp.string === str;
    };
  };

  isList = function(exp, env) {
    return exp instanceof Object && exp.hasOwnproperty('list');
  };

  module.exports = {
    isSymbol: isSymbol,
    symbolOf: symbolOf,
    isString: isString,
    stringOf: stringOf,
    isList: isList
  };

}).call(this);
