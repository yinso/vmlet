// Generated by CoffeeScript 1.4.0
(function() {
  var AST, Environment, Hashmap, Registry, TR, util;

  AST = require('./ast');

  Hashmap = require('./hashmap');

  util = require('./util');

  TR = require('./trace');

  Environment = (function() {

    function Environment() {
      this.inner = new Hashmap({
        hashCode: util.hashCode
      });
    }

    Environment.prototype.has = function(key) {
      return this.inner.has(key);
    };

    Environment.prototype.get = function(key) {
      return this.inner.get(key);
    };

    Environment.prototype.set = function(key, val) {
      return this.inner.set(key, val);
    };

    Environment.prototype.alias = function(key) {
      var ref;
      if (this.has(key)) {
        return this.get(key);
      } else {
        ref = AST.ref(AST.symbol(key.value));
        this.set(key, ref);
        return ref;
      }
    };

    return Environment;

  })();

  Registry = (function() {

    Registry.transform = function(ast) {
      if (!this.reg) {
        this.reg = new this();
      }
      return this.reg.transform(ast);
    };

    function Registry() {}

    Registry.prototype.transform = function(ast) {
      return this._trans(ast, new Environment());
    };

    Registry.prototype._trans = function(ast, env) {
      var type;
      type = "_" + (ast.type());
      if (this[type]) {
        return this[type](ast, env);
      } else {
        throw new Error("clone:unknown_type: " + (ast.type()));
      }
    };

    Registry.prototype._number = function(ast, env) {
      return ast;
    };

    Registry.prototype._string = function(ast, env) {
      return ast;
    };

    Registry.prototype._bool = function(ast, env) {
      return ast;
    };

    Registry.prototype._null = function(ast, env) {
      return ast;
    };

    Registry.prototype._unit = function(ast, env) {
      return ast;
    };

    Registry.prototype._symbol = function(ast, env) {
      var ref;
      ref = env.alias(ast);
      return ref.name;
    };

    Registry.prototype._ref = function(ast, env) {
      return env.alias(ast.name);
    };

    Registry.prototype._define = function(ast, env) {
      var cloned, ref;
      ref = this._trans(ast.name, env);
      cloned = this._trans(ast.value, env);
      ref.value = cloned;
      return AST.define(ref, cloned);
    };

    Registry.prototype._local = function(ast, env) {
      var cloned, ref;
      ref = this._trans(ast.name, env);
      cloned = this._trans(ast.value, env);
      ref.value = cloned;
      return AST.local(ref, cloned);
    };

    Registry.prototype._assign = function(ast, env) {
      var cloned, ref;
      ref = this._trans(ast.name, env);
      cloned = this._trans(ast.value, env);
      ref.value = cloned;
      return AST.local(ref, cloned);
    };

    Registry.prototype._if = function(ast, env) {
      var cond, elseAST, thenAST;
      cond = this._trans(ast.cond, env);
      thenAST = this._trans(ast.then, env);
      elseAST = this._trans(ast["else"], env);
      return AST["if"](cond, thenAST, elseAST);
    };

    Registry.prototype._binary = function(ast, env) {
      var lhs, rhs;
      lhs = this._trans(ast.lhs, env);
      rhs = this._trans(ast.rhs, env);
      return AST.binary(ast.op, lhs, rhs);
    };

    Registry.prototype._member = function(ast, env) {
      var head, key;
      head = this._trans(ast.head, env);
      key = this._trans(ast.key, env);
      return AST.member(head, key);
    };

    Registry.prototype._array = function(ast, env) {
      var item, items;
      items = (function() {
        var _i, _len, _ref, _results;
        _ref = ast.value;
        _results = [];
        for (_i = 0, _len = _ref.length; _i < _len; _i++) {
          item = _ref[_i];
          _results.push(this._trans(item, env));
        }
        return _results;
      }).call(this);
      return AST.array(items);
    };

    Registry.prototype._object = function(ast, env) {
      var key, keyvals, val;
      keyvals = (function() {
        var _i, _len, _ref, _ref1, _results;
        _ref = ast.value;
        _results = [];
        for (_i = 0, _len = _ref.length; _i < _len; _i++) {
          _ref1 = _ref[_i], key = _ref1[0], val = _ref1[1];
          _results.push([key, this._trans(val, env)]);
        }
        return _results;
      }).call(this);
      return AST.object(keyvals);
    };

    Registry.prototype._block = function(ast, env) {
      var item;
      return AST.block((function() {
        var _i, _len, _ref, _results;
        _ref = ast.items;
        _results = [];
        for (_i = 0, _len = _ref.length; _i < _len; _i++) {
          item = _ref[_i];
          _results.push(this._trans(item, env));
        }
        return _results;
      }).call(this));
    };

    Registry.prototype._funcall = function(ast, env) {
      var arg, args, funcall;
      funcall = this._trans(ast.funcall, env);
      args = (function() {
        var _i, _len, _ref, _results;
        _ref = ast.args;
        _results = [];
        for (_i = 0, _len = _ref.length; _i < _len; _i++) {
          arg = _ref[_i];
          _results.push(this._trans(arg, env));
        }
        return _results;
      }).call(this);
      return AST.funcall(funcall, args);
    };

    Registry.prototype._taskcall = function(ast, env) {
      var arg, args, taskcall;
      taskcall = this._trans(ast.funcall, env);
      args = (function() {
        var _i, _len, _ref, _results;
        _ref = ast.args;
        _results = [];
        for (_i = 0, _len = _ref.length; _i < _len; _i++) {
          arg = _ref[_i];
          _results.push(this._trans(arg, env));
        }
        return _results;
      }).call(this);
      return AST.taskcall(taskcall, args);
    };

    Registry.prototype._param = function(ast, env) {
      var name, param;
      name = this._trans(ast.name, env);
      param = AST.param(name, ast.paramType, ast["default"]);
      return param;
    };

    Registry.prototype._procedure = function(ast, env) {
      var decl, name, param, params;
      name = ast.name ? this._trans(ast.name, env) : ast.name;
      params = (function() {
        var _i, _len, _ref, _results;
        _ref = ast.params;
        _results = [];
        for (_i = 0, _len = _ref.length; _i < _len; _i++) {
          param = _ref[_i];
          _results.push(this._trans(param, env));
        }
        return _results;
      }).call(this);
      decl = AST.procedure(name, params);
      decl.body = this._trans(ast.body, env);
      name.value = decl;
      return decl;
    };

    Registry.prototype._task = function(ast, env) {
      var decl, name, param, params;
      name = ast.name ? this._trans(ast.name, env) : ast.name;
      params = (function() {
        var _i, _len, _ref, _results;
        _ref = ast.params;
        _results = [];
        for (_i = 0, _len = _ref.length; _i < _len; _i++) {
          param = _ref[_i];
          _results.push(this._trans(param, env));
        }
        return _results;
      }).call(this);
      decl = AST.task(name, params);
      decl.body = this._trans(ast.body, env);
      name.value = decl;
      return decl;
    };

    Registry.prototype._return = function(ast, env) {
      return AST["return"](this._trans(ast.value, env));
    };

    return Registry;

  })();

  module.exports = Registry;

}).call(this);
