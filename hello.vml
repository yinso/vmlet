define func fib(n) {
  define func helper(i, acc, next) 
    if i == 0 
      acc
    else 
      helper(i - 1, next, acc + next)
  helper(n, 0, 1)
}

console.log('hello world')

define func even(n)
  if n == 0
    true
  else
    odd(n - 1)

define func odd(n) 
  if n == 0
    false
  else
    even(n - 1)


define test = 
  let (y = 2) {
    func test(x) {
      x + y 
    }
  }

export fib, odd, even

